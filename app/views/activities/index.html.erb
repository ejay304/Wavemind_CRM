<h1>Listing activities</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Begindate</th>
      <th>Enddate</th>
      <th>Ref</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @activities.each do |activity| %>
      <tr>
        <td><%= activity.name %></td>
        <td><%= activity.beginDate %></td>
        <td><%= activity.endDate %></td>
        <td><%= activity.ref %></td>
        <td><%= link_to 'Show', project_activity_path(@project,activity) %></td>
        <td><%= link_to 'Edit', edit_project_activity_path(@project,activity) %></td>
        <td><%= link_to 'Destroy', project_activity_path(@project, activity), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Activity', new_project_activity_path(@project,@Activity) %>
